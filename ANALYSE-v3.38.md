# 🔍 تحليل شامل ديال Paraveda CRM (SaaS) — v3.37 → v3.38

**التاريخ:** 2026-09-08 · **المصدر:** `paraveda.rar` (public_html + crm-paraveda-data)

---

## 1. شنو هو المشروع (باش نكونو على نفس الصفحة)

| الجزء | الوصف |
|---|---|
| `public_html/index.html` | التطبيق كامل: React 19 + Tailwind + xlsx مبنيين بـ Vite فملف واحد (1.1 MB). **ما كاينش الـ source code** — كل التعديلات من v3.6 حتى v3.37 تدارو مباشرة على الكود المضغوط (patches). |
| `public_html/api.php` | الـ backend: GET كيرجع كل الداتا، POST كيكتب مفتاح واحد `{key,t,d}` محمي بـ `X-Sync-Token`. |
| `public_html/crm_data.json` | الداتا (203 طلبية، 5 users، 515 مدينة، 871 سطر historique…). |
| `crm-paraveda-data/` | مجلد الداتا برا public_html (backups + audit.log + meta.json) — كيستعملو **api.php آخر** ماشي هاد اللي فالأرشيف. |
| `import.php` / `manual_sync.php` | أدوات مساعدة (استيراد + تحديث يدوي من Digylog). |

**كيفاش كتخدم المزامنة:** كل صفحة كتكتب فـ `localStorage` → `aa(key)` كتصيفط POST للسيرفر → كل 5 ثواني GET وكيتحدثو المفاتيح اللي `t` ديالها أكبر. يعني **localStorage هو المصدر الأول** والسيرفر مرايا.

---

## 2. الأغلاط اللي لقيت (مرتبين حسب الخطورة)

### 🔴 خطيرة — كتمس الداتا

| # | الغلط | الأثر | الحل فـ v3.38 |
|---|---|---|---|
| 1 | **داتا مخربقة فالسيرفر** — `custom_sheets_v1` و `afrizon_team_photos_v1` مخزنين `{t, d: {t, d: ...}}` (غلاف مزدوج). السبب: `import.php` كان كيخزن الغلاف كامل فـ localStorage عوض `d`، ومن بعد التطبيق كيرجعو للسيرفر. | الصفحات المخصصة (صفحة +) وصور الفريق **ما كيتقراوش** — كيرجعو فارغين بلا خطأ ظاهر. | ✔ `ig()` فالتطبيق كتفك الغلاف أوتوماتيك · ✔ `api.php` كيفك الغلاف فالقراءة والكتابة · ✔ `import.php` تصلح · ✔ الداتا فـ `crm_data.json` تنقات. |
| 2 | **حقول LIVRAISON كتمسح مع كل reload** — `F4()` (normaliser ديال الطلبيات) كيعاود يبني كل طلبية بـ 22 حقل فقط. `livreur`, `tracking`, `dateExp`, `dateLiv`, `motif` اللي كتكتبهم فصفحة LIVRAISON **كيتمسحو ملي كتعاود تفتح الصفحة** (ولا ملي كيجي sync من جهاز آخر). | كل Tracking ID / اسم الليفرور / تواريخ الشحن اللي تكتبو ضاعو. الـ history ديال الإنتاج كيأكد: 0 تعديل محفوظ على هاد الحقول. | ✔ `F4()` ولات كتحافظ على 5 الحقول. |
| 3 | **الأدوار (roles) ماشي متطابقة** — التطبيق كيعرف غير `admin` و `user`، ولكن 3 الوكيلات فالإنتاج عندهم `role: "agent"` (جاو من import قديم). | صفحة Users كتبين **0 وكيلات** · ملي كتمسح/تبدل اسم بنت، الحسابات ديالها ما كيتحدثوش · Heatmap/Live ما كيربطوش user بالوكيلة. | ✔ تطبيع أوتوماتيك عند التحميل: أي role ماشي `admin` → `user`. |
| 4 | **زر "🛠️ الإصلاح"** فشاشة الخطأ كان كيمسح من localStorage: `history`, `villes_v2` (أثمنة التوصيل!), `worktimes`, `chat`, `remarques`. إلا كانت نسخة السيرفر أقدم → **خسارة نهائية**. | خطر خسارة أثمنة المدن والهيستوريك. | ✔ كيمسح غير مفاتيح تقنية (`session`, `villes_v1` القديم, `chat_mute`). |
| 5 | **api.php اللي فالأرشيف ضعيف**: بلا قفل (2 كتبو فنفس الوقت → واحد كيضيع)، بلا رفض الكتابات القديمة (tab قديم كيرجع داتا قديمة فوق الجديدة)، backup واحد كيتبدل كل مرة، أي مفتاح كيتقبل، وما فيهش actions ديال Digylog اللي الواجهة كتعيط عليهم (`digylog_sync`, `digylog_test`) → **501**. | تعارض كتابات + استحالة الرجوع للور + Digylog ما خدامش. | ✔ api.php v3.38: flock + رفض stale + backups دوارة (30 كتابة + 30 يوم) + whitelist ديال المفاتيح + حماية من كتابة فارغة (ghost) + audit log. ⚠️ **موديول Digylog ماشي فالأرشيف** — خاص ترجعوه من السيرفر الحي (شوف §4). |

### 🟠 أغلاط حساب / منطق

| # | الغلط | الأثر | الحل |
|---|---|---|---|
| 6 | **التاريخ بـ UTC** — 32 بلاصة كتستعمل `toISOString().slice(0,10)`. المغرب UTC+1 → من **23:00 حتى 00:00** "اليوم" كيكون غالط، وطلبية تزادت 23:30 كتسجل بتاريخ الغد. | Dashboard/Salaire/Work Times/فلاتر "اليوم" و"هذا الأسبوع" غالطين ساعة كاملة كل نهار. | ✔ دالة `__pvLocalDate()` بالتوقيت المحلي فكل 32 بلاصة. |
| 7 | **CA ماشي متناسق بين الصفحات** — COMONDES / Ranking / Salaire / suivi confirmation كيحسبو `CA = Σ prix`، بينما **statistique** و **LIVRAISON** كانو كيحسبو `Σ prix × qte`. فالداتا ديالكم `prix` هو ثمن الطلبية كاملة (2 قطع = 280 DH ماشي 2×280). | statistique كان كيعطي CA و bénéfice **مضاعفين** للطلبيات ديال 2-3 قطع (مثال: 3 قطع × 380 = 1140 عوض 380). | ✔ توحيد: CA = Σ prix فكل الصفحات. |
| 8 | **Frais livraison على Out Of Stock** — LIVRAISON كانت كتحسب فريز على طلبيات "Out Of Stock" (ما تشحناتش). | مجموع الفريز مضخم. | ✔ Out Of Stock = 0 DH بحال Retour/Annulé. |
| 9 | **الوكيلة تقدر تمسح أي طلبية** (غير Livrée محمية). | خطر مسح بالغلط. | ✔ المسح ولا للأدمين فقط. |
| 10 | رقم النسخة فالفوتر `v2.0` بينما INSTALL.txt فـ v3.37. | تشويش. | ✔ `v3.38`. |

### 🟡 ملاحظات ما تصلحاتش (خاصها قرار منكم)

| # | الملاحظة | التوصية |
|---|---|---|
| 11 | **منتوجات مكررة بالكتابة** فالداتا: `"المكمل الغذائي لإنقاص الوزن slim bio "` (بمسافة فالآخر) كتبان منتوج مستقل فالإحصائيات. نفس الشي للمدن: `Casablanca` / `Cassablanca`, `Sale` / `Salé`, `Mekens`… (110 مدينة مختلفة فـ 203 طلبية). | نزيدو trim + تطبيع عند الحفظ، و dropdown للمدن من LES VILLES (بحال ما درنا للمنتوجات فـ v3.22). |
| 12 | **الأمان**: التوكن السري `Rd` والباسووردات كاملين (admin + الوكيلات) موجودين بـ plain text فـ index.html و crm_data.json — أي واحد كيفتح "view source" كيشوفهم. الـ GET ديال api.php **بلا توكن** → أي واحد عندو الرابط كيحمّل كل الزبناء والتيليفونات. | GET يتحمى بالتوكن + هاش ديال الباسووردات + الأدمين يبدل الباسوورد من صفحة Users بعد الرفع. |
| 13 | ~40 KB ديال داتا تجريبية قديمة (`A4`/`b4` — طلبيات فبراير 2026 ديال AFRIZON) باقية مدمجة فالـ bundle بلا ما تستعمل. | تحيد فالبناية الجاية (ما كتضرش، غير حجم). |
| 14 | `afrizon_period_v1` و `afrizon_backup_v1` مفاتيح ميتة فالسيرفر. | تحيدو عند التنظيف. |
| 15 | **ما كاينش source code** → كل تعديل = patch على minified. ننصحو بشدة نعاودو نبنيو المشروع من source (React + Vite) باش التعديلات الجاية تكون آمنة وسريعة. | قرار استراتيجي. |

---

## 3. الاختبارات

```
node dev/test-all-pages.mjs     # كيشغل التطبيق الحقيقي فـ jsdom بالداتا الحقيقية، كيدخل أدمين، كيدوز على 15 صفحة → 0 أخطاء
node dev/test-v3.38-fixes.mjs   # 8 اختبارات regression للإصلاحات (unwrap, roles, LIVRAISON fields, local date, delete guard, CA)
node dev/serve.js               # سيرفر محلي (بلا PHP) باش تشوفو الـ CRM فالمتصفح
```

النتيجة: **15/15 صفحة بلا أخطاء · 8/8 اختبارات خضرين**.

---

## 4. شنو خاصكم تديرو عند الرفع (deploy)

1. ارفعو `public_html/index.html` (v3.38) + Ctrl+F5.
2. **قبل** ما ترفعو `api.php` الجديد: **حمّلو نسخة ديال api.php الحالي من السيرفر** — فيه موديول Digylog (v3.35–v3.37) اللي ماشي فالأرشيف. صيفطوه لينا باش ندمجو فيه إصلاحات v3.38 (unwrap + whitelist). إلا رفعتو api.php ديالنا مباشرة، Digylog غادي يوقف (كيرجع 501 برسالة واضحة، بلا ضرر على الداتا).
3. ارفعو `import.php` الجديد (ما بقاش كيخربق الداتا).
4. (اختياري) شغّلو `import.php` مرة وحدة من كل جهاز باش يتنظف localStorage، ولا غير خليو الـ sync يدير خدمتو — `ig()` الجديدة كتصلح الغلاف المزدوج بوحدها.
